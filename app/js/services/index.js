import {
  getCards,
  getUnlabeledCards,
  loadAllCards,
  updateCards,
} from './cards';
import {
  getLabels,
} from './labels';
import {
  loadAllLanes,
  updateLanes,
} from './lanes';

export default {
  getCards,
  getUnlabeledCards,
  loadAllCards,
  updateCards,
  getLabels,
  loadAllLanes,
  updateLanes,
};
